<resources>

    <!-- Base application theme. -->
    <style name="KusAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/kusToolbarBackgroundColor</item>
        <item name="colorPrimaryDark">@color/kusStatusBarColor</item>
        <item name="colorAccent">@color/kusColorAccent</item>
    </style>

    <style name="KusAppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="KusAppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="KusAppTheme.NoTitle" parent="KusAppTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
